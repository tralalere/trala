export class Update {
    static execute(args: string[]) {
        console.log('update', args);

        // TODO get module(s) and version(s) (flag to add to trllr.json)

        // TODO if no modules update all modules (?) (maybe not)

        // TODO remove current module

        // TODO install module
    }
}
