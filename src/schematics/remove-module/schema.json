{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsTralalereRemoveModule",
  "title": "Tralalere Remove Module Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the module.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": [
    "name"
  ]
}